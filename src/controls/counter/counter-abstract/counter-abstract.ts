import { ElementAbstract } from "../../element-abstract";
import { ElementParams } from "../../element.model";

export abstract class CounterAbstract extends ElementAbstract {
    constructor(params: ElementParams) {
        super();
    }
}