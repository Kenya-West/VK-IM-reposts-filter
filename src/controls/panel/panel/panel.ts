import { ElementParams } from "../../element.model";
import { PanelBase } from "../panel-base/panel-base";

export class Panel extends PanelBase {
    constructor(params: ElementParams) {
        super(params);
    }
}